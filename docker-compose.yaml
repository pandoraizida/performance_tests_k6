version: '1'
services:
  petstore3:
    image: swaggerapi/petstore:unstable
    ports:
      - "8080:8080"

  influxdb:
    image: influxdb:1.8
    volumes:
      - ./influxdb_data:/var/lib/influxdb
    ports:
      - "8086:8086"

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"